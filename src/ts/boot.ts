/**
 * WordPress entry — side-effect IIFE built by arts-wp into
 * src/php/libraries/__SLUG__/__SLUG__.js.
 */
declare const __DEFINE_KEY__: string

if (import.meta.env?.DEV) {
  console.debug(`__SLUG__ ${__DEFINE_KEY__} booting`)
}

export {}
